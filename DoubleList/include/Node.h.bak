#ifndef NODE_H
#define NODE_H
#include "avl_tree.h"
#include "Queue.h"
#include "File.h"
#include<iostream>
#include<cstdio>
#include<sstream>
#include<algorithm>
#include <fstream>
#define pow2(n) (1 << (n))
using namespace std;

class Node {
public:
    Node* next;
    int x;
    int y;
    int z;

    int uzaklikOrjin;
    Node(int, int, int);
    
};

#endif